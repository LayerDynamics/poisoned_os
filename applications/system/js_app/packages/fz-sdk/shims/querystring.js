var hex = "0123456789ABCDEF";
function hexValue(character) { for (let index = 0; index < hex.length; index++) if (hex[index] === character.toUpperCase()) return index; return -1; }
function escape(value) { var text = "" + value, result = ""; for (let i = 0; i < text.length; i++) { var code = text.charCodeAt(i); var safe = (code >= 48 && code <= 57) || (code >= 65 && code <= 90) || (code >= 97 && code <= 122) || text[i] === "-" || text[i] === "_" || text[i] === "." || text[i] === "~"; result += safe ? text[i] : "%" + hex[(code >> 4) & 15] + hex[code & 15]; } return result; }
function unescape(value) { var text = "" + value, result = ""; for (let i = 0; i < text.length; i++) { if (text[i] === "+") { result += " "; } else if (text[i] === "%" && i + 2 < text.length) { var high = hexValue(text[i + 1]), low = hexValue(text[i + 2]); if (high >= 0 && low >= 0) { result += chr((high << 4) | low); i += 2; } else { result += text[i]; } } else { result += text[i]; } } return result; }
function stringify(object) { var result = "", first = true; for (let key in object) { var raw = object[key]; var values = raw && typeof raw === "object" && typeof raw.length === "number" ? raw : [raw]; for (let i = 0; i < values.length; i++) { if (!first) result += "&"; first = false; result += escape(key) + "=" + escape(values[i]); } } return result; }
function parse(input) { var result = {}; if (!input) { return result; } var text = "" + input, start = 0; for (let cursor = 0; cursor <= text.length; cursor++) if (cursor === text.length || text[cursor] === "&") { var pair = text.slice(start, cursor), separator = pair.indexOf("="); var key = unescape(separator < 0 ? pair : pair.slice(0, separator)); var value = unescape(separator < 0 ? "" : pair.slice(separator + 1)); if (result[key] === undefined) { result[key] = value; } else if (result[key] && typeof result[key] === "object" && typeof result[key].length === "number") { result[key].push(value); } else { result[key] = [result[key], value]; } start = cursor + 1; } return result; }
module.exports = { stringify: stringify, encode: stringify, parse: parse, decode: parse, escape: escape, unescape: unescape };
