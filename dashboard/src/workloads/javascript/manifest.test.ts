import { describe, expect, it } from "vitest";
import { clampJavaScriptLimits, validateJavaScriptManifest } from "./manifest";

const valid = { format: 1, id: "org.poisonedos.example", name: "Example", version: "1.0.0", language: "javascript", entrypoint: "src/main.js", runtime: "poison-mjs-1", runtimeApi: 1, firmwareApi: ">=1.0.0 <2.0.0", capabilities: ["storage.project.read"], limits: { heapBytes: 100, wallTimeMs: 100, logBytes: 100, artifactBytes: 100 }, dependencies: "poison-js.lock", servedUi: null };
describe("JavaScript manifest", () => { it("validates and clamps device limits", () => { const manifest = validateJavaScriptManifest(valid); expect(manifest.id).toBe("org.poisonedos.example"); expect(clampJavaScriptLimits(manifest.limits, { heapBytes: 50, wallTimeMs: 60, logBytes: 70, artifactBytes: 80 })).toEqual({ heapBytes: 50, wallTimeMs: 60, logBytes: 70, artifactBytes: 80 }); }); it("accepts an immutable served UI identity", () => { const servedUi = { bundleId: "org.poisonedos.interface", version: "1.2.3", contentSha256: "a".repeat(64) }; expect(validateJavaScriptManifest({ ...valid, servedUi }).servedUi).toEqual(servedUi); }); it("rejects undeclared runtime values", () => { expect(() => validateJavaScriptManifest({ ...valid, runtime: "node" })).toThrow(); }); });
